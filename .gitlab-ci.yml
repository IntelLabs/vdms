image: "ubuntu:20.04"

before_script:
  - apt-get update -qq && apt-get install -y -qq sqlite3 libsqlite3-dev nodejs
  - ruby -v
  - which ruby

build:
  stage: build
  script:
    - ls -l

test:
  stage: test
  script:
    - ls -l
